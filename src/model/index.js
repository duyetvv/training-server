
import validation from '../validation';

export default class Model {
  constructor(props) {
    this.fields = props;
  }

  validate = (model) => {
    const sortFields = [...this.fields].sort();
    const 

  }
}